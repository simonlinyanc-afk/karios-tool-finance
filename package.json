{
  "name": "kairos-finance",
  "version": "1.2.4-alpha",
  "type": "module",
  "description": "Reimbursement management tool with OCR",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "check:qwen-config": "node scripts/check-qwen-config.mjs",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
